# Arquivos gerados para publicacao
dist/
.netlify/
aluguel-deploy.zip
*-deploy-*.zip

# Backups e dados pessoais nunca devem entrar no repositorio
backups/
*-backup-*.json
*.backup.json

# Configuracoes locais e segredos futuros
.env
.env.*
!.env.example

# Arquivos do sistema/editor
.DS_Store
Thumbs.db
*.log

# Pasta de trabalho temporaria
tmp/

# Estado local da CLI do Supabase (criado por `supabase link`).
# Nao contem senha, mas e estado desta maquina: aponta para o projeto
# linkado aqui e nao faz sentido para quem clonar o repositorio.
supabase/.temp/
